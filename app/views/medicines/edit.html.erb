

<h1>Edit Medicine</h1>

<div class="row">
  <div class="span-6">
    <p>Here you can change the attributes of a prior entry:</p>
  </div>
  <div class="span-6">
    <%= form_for @medicine, html: { class: 'form-horizontal' } do |f| %>
      <div class="control-group">
        <%= f.label :name, class: 'control-label' %>
        <div class="controls">
          <%= f.text_field :name %>
        </div>
      </div>
      <div class="control-group">
        <%= f.label :description, class: 'control-label' %>
        <div class="controls">
          <%= f.text_area :description, rows: 8 %>
        </div>
      </div>
      <div class="control-group">
        <%= f.label :time, class: 'control-label' %>
        <div class="controls">
          <%= f.text_field :time %>
        </div>
      </div>
      <div class="control-group">
        <div class="controls">
          <%= f.submit "Save", class: 'btn' %>
        </div>
      </div>
    <% end %>
  </div>
</div>
